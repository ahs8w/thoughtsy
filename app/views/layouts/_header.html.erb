<nav class="navbar navbar-inverse navbar-fixed-top" role="navigation">

  <!-- Brand and toggle get grouped for better mobile display -->
  <div class="navbar-header">
    <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
      <span class="sr-only">Toggle navigation</span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
    </button>
    <%= link_to "Thoughtsy", root_path, class: "navbar-brand", id: "logo" %>
  </div>
  
  <!-- Collect the nav links, forms, and other content for toggling -->
  <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">  
    <ul class="nav navbar-nav navbar-right">
      <li><%= link_to "How it works", about_path %></li> 
    <% if signed_in? %>
      <li><%= link_to "Users",      users_path %></li>
      <li><%= link_to "Thoughts",   posts_path %></li>
      <li><%= link_to "Queue",      queue_path if user_admin? %><li>
      <li class="dropdown">
        <a href="#" class="dropdown-toggle" data-toggle="dropdown">
          <%= current_user.username %> <b class="caret"></b>
        </a>
        <ul class="dropdown-menu">
          <li><%= link_to "Profile", current_user %><span class="ion ion-person"></span></li>
          <li><%= link_to "Settings", edit_user_path(current_user) %> <span class="ion ion-gear-a"></span> </li>
          <li><%= link_to "Messages", user_messages_path(current_user) %> <span class="ion ion-email"></span> </li>
          <li class="divider"></li>
          <li><%= link_to "Sign out", signout_path, method: "delete" %></li>
        </ul>
      </li>
    <% else %>
      <li><%= link_to "Sign in", signin_path %></li>
    <% end %>
    </ul>
  </div>
  <!-- /.navbar-collapse -->
  <div id="js_flash_messages">
    <%= render 'shared/flash_messages' %>
  </div>

</nav>