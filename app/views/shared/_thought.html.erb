<div class="thought" id="<%= thought.id %>">
  <% profile = false unless local_assigns.has_key? :profile %>

  <div class="thought_content">
    <% if profile %>
      <% if thought.instance_of?(Response) %>
        <%= link_to image_tag(thought.image_url(:thumb)), post_path(thought.post) if thought.image.present? %>
        <%= link_to custom_format(thought.content), post_path(thought.post) unless thought.content.nil? %>
      <% else %>
        <%= link_to image_tag(thought.image_url(:thumb)), post_path(thought) if thought.image.present? %>
        <%= link_to custom_format(thought.content), post_path(thought) unless thought.content.nil? %>
      <% end %>
    <% else %>
      <%= image_tag thought.image_url, class: "img-responsive" if thought.image.present? %>
      <%= custom_format(thought.content) unless thought.content.nil? %>
    <% end %>
  </div>

  <div class="thought_author">
    <% if !profile %>
      <%= link_to "#{thought.user.username}", user_path(thought.user) %>
      <span class="lighter"> | <%= thought.user.score %></span>
    <% end %>
    <span class="timestamp pull-right"><%= created_time(thought) %></span>
  </div>
</div>