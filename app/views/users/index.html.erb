<% provide(:page_title, "Users") %>
<% provide(:page_description, "A list of top users") %>

<div class="col-md-4 col-md-offset-4">
  <div class="well"
    <div class="row" id="users_index">
      <h1>Users</h1>
      <div class="panel-focus">
        <%= will_paginate %>
        <ul class="users">
          <%= render @users %>
        </ul>
        <%= will_paginate %>
      </div>
    </div>
  </div>
</div>